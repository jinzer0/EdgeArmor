#!/bin/sh

MSG_FILE="$1"
ATTRIBUTION="Co-authored-by: Codex <codex@openai.com>"

if [ -z "$MSG_FILE" ] || [ ! -f "$MSG_FILE" ]; then
    exit 0
fi

if grep -Fqx "$ATTRIBUTION" "$MSG_FILE"; then
    exit 0
fi

printf '\n%s\n' "$ATTRIBUTION" >> "$MSG_FILE"
